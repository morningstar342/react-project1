- Create New Project:
    npx create-react-app project
    cd project
    npm start

- Other Commands:
    npm start
    npm run build
    npm test
    npm run eject

- cd project && npm start


- ctrl => i : make father to set of elements
    <div>
        <h1></h1>
        <h2></h2>
    </div>


- App.JS or any file .JS need only 1 parent inside it
    <div>
        <h1></h1>
        <h2></h2>
    </div>

https://github.com/morningstar342/react-project1.git

- github public github
    …or create a new repository on the command line
        echo "# react-project2" >> README.md
        git init
        git add README.md
        git commit -m "first commit"
        git branch -M main
        git remote add origin https://github.com/morningstar342/react-project1.git
        git push -u origin main
    …or push an existing repository from the command line
        git remote add origin https://github.com/morningstar342/react-project1.git
        git branch -M main
        git push -u origin main


- (2nd way) source control =>
            publish gitgub => "type name" => public repositery
            => successful publish

- Update:
    scource control => type msg (update name) => click (3lamet sa7)
    => click on (...) => push